# SmartLearnPro / VerbaPath 核心英语学习功能规格与架构分析说明书

本说明书基于对系统全部核心视图（`src/views/`）、全局状态（`src/context/`）和底层服务（`src/services/`）的完整源码深度遍历，对本软件中所有与**英语学习核心业务相关**的功能进行完完整整的梳理与总结。

---

## 一、 核心阅读导入与 AI 认知解析流

该模块负责外部英语材料的导入、格式规整、AI 深度 morphological（形态学）与认知结构分析，并将提取结果沉淀为下游词汇学习与测验的资产。

### 1. 原生材料导入与限制 (`ImportView.jsx`)
*   **输入支持**：支持直接粘贴文本或上传 `.txt` 及 `.pdf` 文件。
*   **导入体积安全线**：程序在前端强制执行上限为 **120,000个字符 (约 1.5MB)** 的文本截断过滤，确保不会在后续 AI 分析中引发 Token 溢出错误，同时兼顾低端移动设备内存。
*   **格式清洗过滤**：清洗原始文本中的控制字符、统一多余的空白行和空格，保留段落结构（`\n\n`），确保排版完美适配后续句子切分。

### 2. AI 认知解析管道与落地存储 (`ai.js` & `db.js`)
通过 `/api/ai` 将材料发送至 AI 代理服务（包含“全面解析”或“定向分析”提示词），完成核心处理：
1.  **AI 词汇自动提取**：自动分析材料中适合用户的核心学术、行业或阶段词汇。每个提取词包含：
    *   `word`（单词本身）
    *   `phonetic`（国际音标）
    *   `definition`（精确中文释义）
    *   `contextSentence`（在导入文章中出现的**原句上下文**，提供情境关联）
    *   `translation`（上下文原句的中文翻译）
2.  **段落与 inline 词汇切分**：对整篇文章的段落、句子进行结构化拆分，以实现单词点击即查和语法树拆解。
3.  **阅读理解题库生成**：提取文章核心事实与论点，自动生成托福/雅思风格的阅读理解题（包含细节理解题、推理题、结构逻辑题等）。
4.  **IndexedDB 写入**：
    *   将解析后的原文章沉淀写入 `SmartLearnDB` (v16) 的 `history` 表中。
    *   将提取出的词汇作为“候选词卡”提供给用户，用户可一键批量导入 `flashcards`（闪卡库）中。

### 3. 笔记深度素材指令 (`notes` 与下游的打通)
在 `NotesView.jsx` 中编写的笔记，如果包含特定前缀指令，会被关联服务提取并灌入翻译/写作等练习模块：
*   `@素材`：标识一段地道的例证、论据，自动归类至写作 workbench 备用。
*   `@翻译例句`：标识一段英汉对照的优美长难句，自动进入翻译挑战库。
*   `@替换词`：以“*A -> B*”格式表示的高阶表达替换（如 *happy -> ecstatic*），会自动为写作拼写诊断提供词汇升级建议。

---

## 二、 词汇与 FSRS 渐进式间隔复习系统

软件的核心基石之一。基于领先的 **FSRS (Free Spaced Repetition Scheduler，自由间隔重复调度)** 记忆算法，实现了高度自适应的离线词汇复习系统。

### 1. FSRS 算法参数与状态机实现 (`AppContext.jsx`)
程序集成全局 FSRS 引擎，默认采用以下黄金记忆参数：
*   `request_retention`（目标记忆保留率）：固定为 **`0.9`** (即确保用户在 90% 的概率能够正确回忆时触发复习)。
*   `decay`（记忆衰减速度）：**`-0.5`**。
*   `factor`（难度因子系数）：**`0.9`**。
*   `maximum_interval`（最大复习间隔）：**`365`** 天。
*   **卡片状态机转换**：
    *   新卡片初始为 `State.New (0)`。
    *   复习打分分级：`Again (1, 遗忘)`、`Hard (2, 困难)`、`Good (3, 良好)`、`Easy (4, 简单)`。
    *   复习后状态在 `Learning (1)`，`Review (2)` 和 `Relearning (3)` 之间智能跃迁，自动计算最新的 `stability`（记忆稳定性）、`difficulty`（认知难度值）、`retrievability`（记忆可提取性）和下次复习时间戳 `nextReview`。

### 2. 卡片底层 IndexedDB 数据模型 (`db.js`)
闪卡在 IndexedDB `flashcards` 表中存储，核心字段完全兼容 FSRS 和学习历史追踪：
```json
{
  "id": "UUID (唯一标识)",
  "front": "英文单词/短语",
  "back": "音标、中文释义、例句等",
  "folderId": "文件夹 UUID 关联",
  "tags": ["标签数组"],
  "nextReview": "下一次复习时间戳 (毫秒)",
  "lastReview": "上一次复习时间戳 (毫秒)",
  "interval": "复习间隔天数",
  "repetitions": "复习总次数",
  "lapses": "遗忘/答错总次数",
  "state": "当前状态代码 (0-3)",
  "difficulty": "浮点数 (认知难度值)",
  "stability": "浮点数 (记忆稳定度)",
  "weaknessScore": "薄弱度综合指标 (答错率与天数加权)",
  "createdAt": "创建时间戳"
}
```

### 3. 多元复习模式与队列调度 (`ReviewCenterView.jsx`)
*   **文件夹物理组织**：允许用户自定义多级文件夹结构，并自动管理 `"Daily - YYYY-MM-DD"` 等每日自动同步文件夹。
*   **标准复习队列**：拉取 `nextReview <= 当前时间` 且处于活跃状态的所有卡片，按记忆可提取性升序排列，将最濒临遗忘的卡片优先推给用户。
*   **弱项深度复习 (Deep Review)**：专门拉取 `weaknessScore` 极高的卡片进行对抗式强化。
*   **极速闪击 (Quick Fire)**：不改变卡片 FSRS 调度状态，仅供考前或碎片化时间进行中英互译速记，过滤掉调度逻辑。

---

## 三、 阅读深度解析与对抗式真题引擎

该模块使用户不仅能够“读”，更能深入段落和词汇内部，甚至生成极具挑战性的对抗性试题。

### 1. 交互式词汇/长难句切分解析 (`ExamView.jsx`)
*   **点击即查**：使用精准的英文单词分词正则表达式，将文章切分为可交互的独立单词单元。点击任何单词，过滤标点符号后立即调用本地/在线词典进行查词。
*   **长难句拆解**：允许用户点选任意句子。调用 AI 分析，产出结构极清晰的语法树，标明：
    *   **主干结构 (Main Clause)**：主谓宾/主系表核心。
    *   **修饰成分 (Clauses)**：定语从句、状语从句、名词性从句。
    *   **关键短语 (Key Phrases)**：固定搭配与学术习语。

### 2. 对抗式阅读试题生成 (`ai.js`)
*   **对抗性命题机制**：与普通阅读理解不同，系统使用专门提示词命令 AI 根据导入的英文材料生成**混淆度极高**的客观选择题，重点测试用户的**逻辑谬误、过度概括、因果倒置、偷换概念**等中高级英语阅读盲区。
*   **严密的数据响应格式**：要求 AI 强制返回严格的 JSON 格式，其中包含：
    *   `questions`（选择题列表，包含 A/B/C/D 四个选项）
    *   `correctAnswer`（正确答案）
    *   `explanation`（深度的中文干扰项剖析，解释为什么其他三个选项虽然看起来像，但其实是陷阱）
    *   `evidence`（摘自原文的直接依据）

### 3. 智能评分与反馈系统
*   **标准化换算**：支持四六级考试（0-15分量表）和雅思（IELTS 0-9分波段）两种权威标准的换算。
*   **答题数据分析**：记录做题时长、正确率趋势，并将错题分类归纳为“细节推理偏差”、“词汇误解”、“主旨把握偏差”等统计维度，展示在仪表盘。

---

## 四、 沉浸式口语教练与语音闭环

通过高灵敏度音频捕获与流式应答管道，构建了无障碍的真人级英语口语对话环境。

### 1. 音频捕获与流式架构 (`CoachView.jsx`)
```
[麦克风录音] -> MediaRecorder 捕获 -> Wav/Mp3 二进制 Blob -> STT 语音转文字接口 ->
文本提示词结合 (口语教练人设 + 历史记忆) -> AI 生成英文文本回答 -> TTS 文字转语音接口 ->
获取二进制音频流 -> 前端 Audio 播放 -> [用户耳机/扬声器]
```

### 2. 语音转文字 (STT) 与文字转语音 (TTS) 配置
*   **STT 引擎适配**：调用 `/api/audio` 进行高精度转写，专为非母语者口语发音优化，支持捕捉发音中的含混和连读。
*   **TTS 拟真发音**：调用 `/api/tts`。用户可一键切换英音 (UK) 或美音 (US)、男声或女声，并可自由调节播放速度倍率（支持 **`0.8x` 慢速细听**、`1.0x` 标准、`1.2x` 快速挑战）。

### 3. 智能口语诊断反馈卡
在语音对话的同时，AI 在后台静默对用户的转写文本进行语言学分析，生成可视化评估报告：
*   **语法纠错**：实时捕获主谓不一致、时态混乱、介词误用等口语常见语法病句，给出修正对比。
*   **词汇升级**：检测用户口语中频繁重复使用的简单词（如 *nice, good, bad*），推荐更符合当下语境的高级近义词。
*   **发音提示**：根据发音混淆点给出单词重音、连读和失去爆破的视觉化指导。

---

## 五、 多功能听力实验室

针对听力“盲听、听写、精听”的多维训练场所，具备强大的本地音频实时剪辑和时戳同步对齐技术。

### 1. 音频解析与本地片段剪切 (`audioUtils`)
*   **上传支持**：支持最大 **25MB** 的本地音频直接导入，永久储存于 IndexedDB `fileLibrary` 中，免去网络延迟。
*   **音频无损切片**：
    *   用户在音轨波形图上选中起止时间戳（秒级）。
    *   前端使用 Web Audio API 解码为 `AudioBuffer`。
    *   通过 `sliceAudioBuffer` 截取选定帧段，然后通过 `audioBufferToWav` 在本地将其转换为全新的 WAV 音频片段（例如：`[片段]-Lesson1-00_12-00_25.wav`）。
    *   用户可以对切出的困难片段进行无限循环播放。

### 2. 听写训练模式 (`ListeningView.jsx`)
*   **挖空听写 (Partial Dictation)**：系统根据字幕时间戳，隐藏句子中的部分核心实词（如动词、名词、形容词），用户必须边听边填空。
*   **全文听写 (Full Dictation)**：用户盲听整句并敲入完整英文文本。系统运用高效的**文本字符差异比对算法 (Diff Heuristics)**，以极高精度用不同颜色标红标绿：
    *   多写/赘余的单词
    *   拼写错误的字符
    *   漏听/缺失的词汇

### 3. 字幕时间轴高亮同步与 A-B 重复
*   **Subtitle Synchronization**：解析 `.srt` 或 `.vtt` 字幕。当音频播放器触发 `onTimeUpdate` 事件时，系统精准计算当前毫秒数，自动滚动并将正在发音的英文字幕行置顶高亮。
*   **A-B 复读与句子复读**：支持句级自动暂停（听完一句自动暂停，给用户预留跟读/默写时间）及 A-B 段落无限循环。

---

## 六、 翻译教练挑战与智能写作 Workbench

此模块旨在解决英语“输出”侧的痛点，结合了“先词后句、循序渐进”的翻译闭环以及“多维度诊断”的写作工坊。

### 1. 翻译教练：双重修改润色机制 (`TranslationChallengeView.jsx`)
*   **智能弱词植入**：自动拉取用户生词库中遗忘最严重的词汇（`weaknessScore >= 10`）作为本次翻译的“目标硬骨头词”，强制在当前翻译任务中进行运用。
*   **“两轮提交”教练模式**：
    1.  **Draft 1 (初稿翻译)**：用户凭直觉提交第一版译文。
    2.  **渐进式提示系统 (Hints Engine)**：AI 并不直接给出答案，而是生成三层提示：
        *   *提示一*：检查关键信息是否缺失（语义完整性度量）。
        *   *提示二*：语法时态及核心搭配检查。
        *   *提示三*：句式丰富度微调方向。
    3.  **Draft 2 (润色重写)**：用户结合提示提交第二版。系统对两版译文进行评分比对，给出具体的**“润色得分收益 (Revision Gain)”**（如：初稿 70 分 -> 二稿 92 分，收益 +22分），极大鼓励学习成就感。
*   **阶梯步进模式 (Scaffolded Mode)**：
    *   *Step 1 (短语关)*：先翻译并检测核心词组。
    *   *Step 2 (填空关)*：给出半开放式句架，让用户补全关键结构。
    *   *Step 3 (全句关)*：进行完整翻译，逐步构建信心。

### 2. 写作工坊：15分制专业评估与修改系统 (`WriterView.jsx`)
*   **AI 题纲规划器**：支持 Argumentative（议论文）、Expository（说明文）、Narrative（记叙文）、Business Email（商务邮件）等文体。AI 自动为用户生成严密的段落大纲 and 论点引导。
*   **雅思/四六级 15分量表多维评分**：
    *   **Task Response (内容完整切题性)**：0-4分。
    *   **Coherence & Cohesion (逻辑衔接连贯性)**：0-4分。
    *   **Lexical Resource (词汇丰富与地道度)**：0-3.5分。
    *   **Grammatical Range & Accuracy (语法多样与精确性)**：0-3.5分。
*   **拼写与语法一键修复**：AI 扫描全文，找出拼写错误、标点失误以及高级语法漏洞，生成对比卡片，用户点击“应用修改”即可一键把修正后的内容覆盖至文本编辑器中。

---

## 七、 可视化画布学习流自动化引擎

这是一种极具前瞻性的功能，将死板的学习流程变为了**图形化、节点化的自动化管道**。

### 1. 画布节点拓扑与边配置 (`LearningFlowCanvasView.jsx`)
*   **持久化机制**：画布的节点（`nodes`）与连线关系（`edges`）储存在 `localStorage` (`smartlearn_flow_canvas`)。
*   **连线渲染**：利用 SVG Canvas 层渲染高平滑度的三次贝塞尔曲线（Cubic Bezier Curves），并支持交互连线拖拽。

### 2. 五大英语学习核心节点类型与执行机制
1.  **AI Brain 节点 (`ai_brain`)**：作为智能流的“大脑”。它会自动分析相邻输入节点的内容和用户近 7 天的学习数据，生成本学习流的“最优行动指南”，并将指令广播给下游节点。
2.  **Agent Tool 节点 (`agent_tool`)**：可执行独立业务动作的节点。这些节点直接与 `agentTools.js` 中声明的离线工具（如 `create_flashcards` 批量创卡、`create_listening_audio` 生成听力、`review_flashcards` 等）对接。
3.  **Tool Bundle 节点 (`tool_bundle`)**：工具集节点，负责在单个节点内事务性地、串行执行多个动作（例如：*提取生词 -> 自动建卡 -> 划归文件夹* 一步到位），并输出日志。
4.  **Note Composer 节点 (`note_composer`)**：自动聚合所有连接的父节点的数据资产，利用 AI 自动融会贯通，生成一篇排版精美、结构严密的“深度复习笔记”，自动沉淀进 `notes` 库。
5.  **Practice Pack 节点 (`practice_pack`)**：练习包生成节点。输入一篇文章或音轨后，自动将其中的重难点生成为由**闪卡、写作练习句、翻译例句**组成的综合测试包，直接写入相应的离线数据库。

---

## 八、 语义词汇关联知识图谱

打破传统的字母表背单词模式，构建了立体的“记忆突触”网络。

### 1. 四重核心语义关联算法 (`KnowledgeGraphView.jsx`)
图谱不仅渲染单词，还会根据复杂的数学 and 语言学规则自动计算并建立节点之间的物理连线（Edges）：
1.  **物理文件夹归属（Folder Association）**：同文件夹的单词天然与文件夹中心节点相连。
2.  **编辑距离形似词（Spelling Link）**：计算单词之间的 **Levenshtein Distance（莱文斯坦距离）**。单词长度大于 3 且距离 `<= 2` 时（短单词 `<= 1`），自动建立“形似词”边。
3.  **中文释义近义词（Synonym Link）**：对单词的中文释义进行分词（剔除“的、在”等停用词）。当两个单词共享至少 1 个具有实际意义的核心中文键词时，建立“近义词”边。
4.  **AI 深度语义网（High-Dim Semantic Link）**：调用 `generateKnowledgeGraphReferences` 服务，让大模型分析单词之间的深层语境相关性或概念关联（如同属“经济学”或“太空探索”主题），建立高级语义连线。

### 2. Spaced Repetition 记忆热力图与动效
*   **基于 FSRS 记忆状态的着色体系**：
    *   **已掌握节点 (稳定性极高，可提取性 > 80%)**：亮绿色 (`#4ade80`)。
    *   **学习中节点 (中等记忆强度，40% - 80%)**：明黄色 (`#facc15`)。
    *   **薄弱节点 (濒临遗忘或错误频发，< 40%)**：绯红色 (`#f87171`)。
*   **力导向粒子模拟**：基于 HTML5 Canvas 和 `react-force-graph-2d` 进行力导向网络运动模拟（Force Simulation）。选中某单词时，图谱会泛起涟漪，激活的关联路径上会有粒子加速流动，直观展现大脑词汇记忆网络的强弱连接。
