# Public frontend setting. This value is embedded into the built JS bundle,
# so put only your own backend address here, never a model provider API key.
VITE_API_PROXY_ORIGIN=http://139.224.210.180:3001
