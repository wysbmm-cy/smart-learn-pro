# Optional public frontend setting. This value is embedded into the built JS bundle.
# Leave it unset for the public app. Never put a model provider API key here.
# VITE_API_PROXY_ORIGIN=https://your-own-backend.example.com
